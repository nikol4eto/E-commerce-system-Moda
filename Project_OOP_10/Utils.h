#ifndef UTILS_H
#define UTILS_H
#pragma once
#include "String.h"
using namespace std;

bool str_equal(const String a, const String b);
bool starts_with(const String str, const String prefix);

#endif